/*
import itmo.p3108.chain.HandlerNoArgument;
import itmo.p3108.command.Info;
import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;

public class HandlerNoArgumentTest {
    HandlerNoArgument noArgumentHandler;


    @Test
    public void check_noArgument_handler() {
        noArgumentHandler = new HandlerNoArgument();
        String result = noArgumentHandler.processRequest(new Info());
        Assertions
                .assertThat(result.length())
                .as("Server:noArgumentHandler can't execute command")
                .isGreaterThan(1);
    }
}
*/
